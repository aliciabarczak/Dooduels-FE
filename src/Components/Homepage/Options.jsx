import "./../Options.css";
const Options = () => {
  return (
    <div className="Homepage-Options">
      <p>Add Room</p>
      <form>
        <label>
          <input type="text" placeholder="Search room..." />
        </label>
      </form>
    </div>
  );
};

export default Options;
